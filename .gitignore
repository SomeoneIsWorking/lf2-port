# Upstream installer and everything extracted from it — never commit game assets.
LF2_v2.0a.exe
game/
scratch/
.venv/
__pycache__/
# The port's settings file (issue #70): a user's bindings are their own.
lf2.cfg

# Wine oracle prefix and run artifacts

# Ghidra dump of the game code: every instruction with its raw bytes, which is enough
# to reconstruct .text. A derivative of a binary this repository does not distribute.
# Regenerate it from your own copy -- see docs/isa-scope.md.
re/instructions.tsv

# ctest writes this when it is run from the repo root instead of the build dir.
Testing/
